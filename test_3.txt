Baky programa;

var int i,j;

function int func(int x, int o)
var int i;
{
    i = 3;
    write(5+2);
    return i;
}

void Baky()
var int j, i;
var boolean pasa;
{
    from i to (6-1) do {
        write("for");
        if(false) {
            pasa = true;
        }
    }

    if(pasa){
        read(j);
    }
    if(pasa || 34 > 4 && false) {
        i = i * 2;
    }
    else {
        i = i+2;
    }

    while(j>8){}
    write("done for");
    func(1, 21);
}